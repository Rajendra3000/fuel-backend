{
  "name": "fuel-sales-backend",
  "version": "1.0.0",
  "description": "OCR backend for the Fuel Station Manager app. Accepts a base64 slip photo, runs it through OCR.space, and extracts MS/HSD CumVol readings.",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
